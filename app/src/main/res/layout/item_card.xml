<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/app_secondary_margin"
    android:padding="@dimen/app_secondary_padding">

    <TextView
        android:id="@+id/tvCardName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:textSize="@dimen/task_list_screen_secondary_text_size"
        tools:text="Episode A"
        android:textColor="@color/app_primary_text_color"
        android:fontFamily="@font/alegreya_sans"/>

    <TextView
        android:id="@+id/tvCardCreatorName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tvCardName"
        android:layout_marginTop="@dimen/app_secondary_padding"
        android:textSize="@dimen/task_list_screen_tertiary_text_size"
        tools:text="created by Jayesh"
        android:textColor="@color/app_secondary_text_color"
        android:fontFamily="@font/alegreya_sans"/>

</androidx.constraintlayout.widget.ConstraintLayout>